#include <iostream>
#include "UPilaL.h"
#include "UPilaV.h"

using namespace std;

int main() {
	PilaV *p = new PilaV();
	p->meter(1);
	p->meter(2);
	p->meter(20);
	p->meter(7);
	p->meter(2);
	p->meter(10);
	cout << p->to_str() << endl;
	cout << p->cima() << endl;
	system("pause");
	return 0;
}
